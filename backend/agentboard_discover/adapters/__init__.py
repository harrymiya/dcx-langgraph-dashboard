"""Built-in Agent Board discovery adapters.

Each adapter owns one Agent's process matching and local data-source parsing.
The discovery entrypoint only coordinates adapters and publishes their cards.
"""
